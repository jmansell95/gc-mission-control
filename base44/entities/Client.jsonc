{
  "name": "Client",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Client company name"
    },
    "division_id": {
      "type": "string",
      "description": "The division this client belongs to. Set automatically from the active division when the client is created. Drives division-scoped filtering \u2014 a division only sees its own clients. Blank = unassigned (visible to enterprise admins only)."
    },
    "parent_client_id": {
      "type": "string",
      "description": "Parent group client ID \u2014 links operating entities (e.g. Concept Engineering, CGL) to their parent holding group (e.g. Phenna Group). When set, this client is displayed as a subsidiary of the parent group in job cards, workload widgets, and financial rollups. Blank for standalone clients and for the parent group record itself."
    },
    "is_holding": {
      "type": "boolean",
      "default": false,
      "description": "True when this client is a parent/holding group (e.g. Phenna Group) that owns operating entities. Holding groups are not typically direct job clients \u2014 jobs are linked to their subsidiary operating entities. Used to distinguish the parent in the Workload Ownership widget and client hierarchy views."
    },
    "contact_name": {
      "type": "string",
      "description": "Primary contact person (legacy single-contact field, kept for backward compatibility). Prefer the 'contacts' array for multiple contacts."
    },
    "contact_email": {
      "type": "string",
      "description": "Contact email (legacy single-contact field)."
    },
    "contact_phone": {
      "type": "string",
      "description": "Contact phone number (legacy single-contact field)."
    },
    "contacts": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Contact person name"
          },
          "role": {
            "type": "string",
            "description": "Contact role/job title (e.g. Project Manager, Accounts)"
          },
          "phone": {
            "type": "string",
            "description": "Contact phone number"
          },
          "email": {
            "type": "string",
            "description": "Contact email address"
          },
          "notes": {
            "type": "string",
            "description": "Optional notes about this contact"
          }
        },
        "required": [
          "name"
        ]
      },
      "default": [],
      "description": "All contacts for this client \u2014 unlimited, no primary designation. Each entry has name, role, phone, email, notes. Used for comms and portal invite recipient selection."
    },
    "yard_address": {
      "type": "string",
      "description": "Address of the client's yard, depot or collection point \u2014 where GC crews collect or return gear, materials or samples. Used as the geocode reference for the lat/lng fields."
    },
    "lat": {
      "type": "number",
      "description": "Client yard/depot latitude. Set via the geocode button on the client form. Used for geofence detection \u2014 when a vehicle enters the radius around this location, an arrival event is triggered (e.g. collecting gear from Concept's yard)."
    },
    "lng": {
      "type": "number",
      "description": "Client yard/depot longitude. Set via the geocode button on the client form. Used for geofence detection."
    },
    "geofence_radius_override": {
      "type": "number",
      "description": "Per-client geofence radius override (metres). When set, this radius is used instead of the global default from the geofence config. Useful for large client yards that need a wider zone."
    },
    "is_partner": {
      "type": "boolean",
      "default": false,
      "description": "True when this client is a partner consultancy that subcontracts work to us (e.g. Concept Engineering Consultants). Partner jobs are visually differentiated on cards, boards and dashboards with a distinct badge and workload split."
    },
    "partner_color": {
      "type": "string",
      "description": "Brand accent colour (hex) for this partner client's badges and dashboard widgets. Defaults to a brand-appropriate colour when not set."
    },
    "portal_user_id": {
      "type": "string",
      "description": "The platform user ID linked to this client's contact for secure portal login. Set when the client is invited via the secure-login mode in the job Links tab. When set, the user can log in and see all jobs where they are the client."
    },
    "portal_invited_at": {
      "type": "string",
      "description": "ISO timestamp of when the secure portal login invite was last sent to this client's contact."
    }
  },
  "required": [
    "name"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "data.division_id": "{{user.data.division_id}}"
        },
        {
          "data.division_id": null
        },
        {
          "data.division_id": {
            "$in": "{{user.data.managed_division_ids}}"
          }
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "is_enterprise_admin": true
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}